//test1

program test1

int i;
bool ijedva;

int niz[];
{
    void pris(int a, int b){
        print(a);
        print(b);
    }
    int sum(int z, int d)
    {
        return z + d;
    }

    int inkrement(int a) int pom; 
    {
        pom = 1;
        return sum(a,pom);
    }

    int fib(int n){
        if(n == 0 || n==1)
            return 1;
        else
            return fib(n-1) + fib(n-2);
    }

    void main()
    {
        pris(0,1);
        niz = new int[3];
        i = 2;
        niz[0] = sum(i, 5);
        niz[2] = inkrement(niz[0]);

        print(len(niz));
        print(chr(97));
        print(ord('a'));

        print(niz[0]);
        print(' ');
        print(niz[2]);
        if(1!=1) print(-6);
        else print(0);  

        ijedva = true;
        print(eol);
        print(fib(20));
        for(i = 0;i < 500;i++){
            if((i % 2) == 0) print(i);
        }
        if(ijedva)
            if(i == 5) print(1); 
            else print(-1);

        niz = new int[3];
        niz[0] = 1;
        niz[1] = 2;
        niz[2] = sum(niz[1], niz[0])  + 1;
        if(niz[2] == 4) print(3); 
        else print(-3);

        i = 0;
        for(; i < 10;){
            if(i == 3){
                i++;
               continue;
            }
            if(i == 4) break;
            i = inkrement(i);  
        }
        if(i == 3) print(4);
        else print(-4);  

        read(niz[0]);
        read(niz[1]);
        read(niz[2]);
        print(eol);

        print(sum (sum(niz[0], niz[1]), niz[2])); 
    }
}

